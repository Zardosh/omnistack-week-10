import React from 'react'; // Necessário para ter HTML dentro do JS
import ReactDOM from 'react-dom'; // Interagir com a DOM no browser
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));

// JSX (JavaScript + XML)